---
title: Shell Scripting
description: Run shell scripts in the browser — command execution, file manipulation, text processing, and pipe chaining via jiki.
category: runtime
features:
  - Shell command execution
  - File and directory manipulation
  - Text processing
  - Script execution
---

import { Demo } from "../../../../../examples/shell-scripting/src/Demo";

<div className="demo-frame">
  <Demo showHeader={false} client:load />
</div>

<div className="example-prose">

A shell scripting environment for running bash/sh commands, manipulating files and directories, and executing scripts. Backed by jiki's shell capabilities with a full terminal interface.

## What's inside

- Standard commands like `ls`, `cat`, `echo`, and `mkdir`
- File creation, reading, writing, and deletion from the shell
- Pipes and basic text manipulation
- Shell scripts and Node.js files can both be executed

## Explore

1. Type shell commands in the terminal and see the output
2. Navigate the file system with `ls` and `cat`
3. Create new files with `echo` and redirection

</div>
